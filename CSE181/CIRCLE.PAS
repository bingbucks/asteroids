PROGRAM Circle;
CONST
     pi=3.14;
VAR
   R:Real;
   C:Real;
   A:Real;
BEGIN
     Writeln('Enter the radius');
     Readln(R);
     C:=R*2*pi;
     A:=r*r*pi;
     Writeln('The circumference is ',C);
     Writeln('The area is ',A)
END.
